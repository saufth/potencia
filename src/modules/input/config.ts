// Types
import type { ButtonTypesConfig, OCStateConfig } from '@/types/input'

/** Button types configuration */
export const BUTTON_TYPES: ButtonTypesConfig = {
  button: 'button',
  submit: 'submit',
  reset: 'reset'
}

/** Menu state configuration */
export const OC_STATE: OCStateConfig = {
  open: 'open',
  closed: 'closed'
}
